package SimilarityMeasures;

import org.junit.Test;

import java.util.Map;
import java.util.Set;
//import static SimilarityMeasures.CosineSimilarity.cosineSimilarity;
import static org.junit.Assert.*;

public class CosineSimilarityTest {

    @Test
    public void calculateCosineSimilarity() {
        final Map<CharSequence, Integer> leftVector;
        final Map<CharSequence, Integer> rightVector;
//        Double result = cosineSimilarity(leftVector, rightVector);
//        final Set<CharSequence> intersection = getIntersection(leftVector, rightVector);
//        final double dotProduct = dot(leftVector, rightVector, intersection);
    }
}